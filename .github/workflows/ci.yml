name: CI

on:
  push:
  pull_request:

jobs:
  public-proof-check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Check required public files
        run: |
          test -f README.md
          test -f public_proof_manifest.md
          test -f safe_public_scope.md
          test -f redaction_checklist.md
          test -f seven_day_github_balance_plan.md

      - name: Check public boundary wording
        run: |
          ! grep -R --exclude-dir=.git --exclude-dir=.github "Maximilion Public Proof" .
          ! grep -R --exclude-dir=.git --exclude-dir=.github "maximilion-public-proof" .
          ! grep -R --exclude-dir=.git --exclude-dir=.github "private Maximilion" .
          ! grep -R --exclude-dir=.git --exclude-dir=.github "Public Maximilion" .
          ! grep -R --exclude-dir=.git --exclude-dir=.github "private Maximilion internals" .
